from setuptools import setup, find_packages

setup(
    name="odin",
    version="0.0.1",
    zip_safe=False,
    author="burnsca@amazon.com",
    long_description="Create an Object Detection inference model from 3D images of classes",
    python_requires='>=3.6'
)